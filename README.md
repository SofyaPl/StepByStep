# ШагЗаШагом (StepByStep)

Персональный ежедневный задачник как Progressive Web App: день, статусы, повторения без накопления долгов, синхронизация с собственной Google Таблицей. Без аккаунта в стороннем сервисе, без подписки, без канбан-досок.

[![GitHub Pages](https://img.shields.io/badge/Live-GitHub%20Pages-22c55e?style=for-the-badge&logo=github)](https://sofyapl.github.io/StepByStep/)
[![PWA Ready](https://img.shields.io/badge/PWA-Installable-6366f1?style=for-the-badge&logo=pwa)](https://sofyapl.github.io/StepByStep/)
[![React 18](https://img.shields.io/badge/React-18-61dafb?style=for-the-badge&logo=react)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5-3178c6?style=for-the-badge&logo=typescript)](https://www.typescriptlang.org/)
[![Tailwind CSS](https://img.shields.io/badge/Tailwind-3.4-38bdf8?style=for-the-badge&logo=tailwindcss)](https://tailwindcss.com/)

**Живая версия:** [https://sofyapl.github.io/StepByStep/](https://sofyapl.github.io/StepByStep/)

---

## Зачем это

Коммерческие планировщики обычно тянут иерархию проектов, теги, геймификацию и paywall. Нужно было другое: открыть день на телефоне, записать дело вместе с комментарием, отметить, перенести, настроить повтор для уколов / полива / дней рождения и не потерять историю между устройствами.

Данные живут локально в браузере. Облако — обычная Google Таблица в своей учётке, к которой приложение ходит через опубликованный Apps Script.

---

## Как это сделано

Автор — не разработчик по профессии. Код писался в Cursor: постановка задачи, разбор багов, правки синхронизации и деплой шли в диалоге с агентом, а не из готового ТЗ.

Это важно для чтения кода. Решения вроде гонок автосинка, `useRef` вместо замыканий и реестра удалённых id появились не как «архитектура с нуля», а как ответы на конкретные поломки: задачи воскресали после удаления, синхронизация крутилась каждую секунду, PWA на Windows теряла `localStorage` из‑за очистки cookie при закрытии окна.

---

## Что умеет приложение

### День как единица планирования

- Горизонтальная лента дат (`DateStrip`): сегодня подсвечен, на каждом дне — счётчик задач.
- Статусы: в плане, сделано, отменено. Выполненные в фильтре «Все» уходят вниз списка.
- Пропущенный эпизод цикла (`missed`) попадает во вкладку «Отмена», а не копится в плане на сегодня.
- Прогресс дня — доля завершённых от общего числа на выбранную дату.
- Разовую задачу можно перенести на завтра или на произвольный день.

### Повторения без снежного кома долгов

Типичный трекер при пропуске ежедневного дела копит просрочку: вчера не сделал зарядку — сегодня висят две. Через неделю — семь. Для курса уколов «каждые 2 дня» или полива это ломает смысл расписания.

Здесь правило другое:

| Ситуация | Что происходит |
|---|---|
| Ежедневный цикл, вчера не сделали | Сегодня — один сегодняшний эпизод. Вчерашний помечается `missed`, в план сегодня не тащится. |
| «Каждые N дней», сегодня дня цикла нет | Вчерашний незакрытый эпизод может всплыть как просрочка с предложением перенести. Когда наступает следующий день цикла, старый долг больше не предлагается. |
| Закрыли сегодняшний эпизод серии | Предшествующие незакрытые эпизоды той же серии закрываются сами. |

Режимы повтора:

- каждый день;
- каждые N дней (1, 2, 5 — курсы медикаментов, полив);
- выбранные дни недели;
- ежегодно;
- опциональная дата окончания или бессрочно.

Серию можно остановить с выбранного дня, превратить цикл в разовую задачу (текущий день остаётся, будущие pending-экземпляры снимаются), поменять правило или удалить: только этот день / будущее / весь цикл.

Разовые просрочки ведут себя иначе: янтарная плашка предлагает перенести их на сегодня. Цикл туда не смешивается, если на сегодня уже есть плановый эпизод.

### Local-first и Google Таблица

Задачи пишутся в `localStorage` сразу. Сеть нужна только для синка.

Бэкенд — Google Apps Script (`doGet` / `doPost`) над первым листом книги. Шаблон скрипта копируется из настроек приложения. Таблица — источник правды между телефоном и компьютером; правки руками в строках тоже подтягиваются.

Автосинк после локальных правок — с дебаунсом 3 секунды. Параллельный повторный запрос блокируется; если пользователь успел ещё что-то изменить, результат сети не затирает локальный список, а мержится.

Фоновая проверка при возврате в окно тихая и с кулдауном 2 минуты, чтобы клик по PWA на Windows не крутил спиннер зря.

### PWA

`display: standalone`, иконки `any` + `maskable`, `start_url` / `scope` под GitHub Pages (`/StepByStep/`). На Windows/Chrome ставится отдельным окном; на Android — WebAPK; на iOS — «На экран Домой» из Safari.

Service worker (Workbox через `vite-plugin-pwa`) кэширует сборку. Обновление: `skipWaiting` + `clientsClaim`, проверка при `visibilitychange`, кнопка принудительной очистки кэша в настройках.

На десктопе Chromium может стирать `localStorage` вместе с cookie при закрытии окон. Если связь с таблицей пропадает после перезапуска PWA — в Chrome: `chrome://settings/content/siteData` → разрешить `https://sofyapl.github.io`.

---

## Конфликт «удалено локально vs ещё есть в таблице»

Обычный merge по id ломается так: пользователь удалил задачу → локального id нет → GET из таблицы возвращает старую строку → задача появляется снова.

Поэтому отдельно хранится карта `step_by_step_deleted_ids_v1`: `id → ISO-время удаления` (записи старше 30 дней вычищаются). В распределённых системах такой маркер называют tombstone: «этого id больше нет, даже если удалённая копия ещё где-то лежит».

При синке (`src/services/googleSheets.ts`):

1. Строка из таблицы с id из карты **не возвращается** в UI и не должна снова записаться как живая задача.
2. Если удалён корень цикла, отбрасываются и дочерние id (`rec-{parentId}-{YYYY-MM-DD}` и `recurrenceParentId`).
3. Если локально id нет, а в таблице задача есть, смотрим `lastSyncedAt`: создана и не менялась до прошлого синка — считаем локальным удалением и дописываем id в карту. Создана или обновлена после синка — это новое с другого устройства или правка в таблице, берём её.
4. Симметрично: задача есть локально, в таблице её уже нет, и она не менялась после прошлого синка — удалили в таблице, в карту, локально не оставляем.

Без карты «пустой локальный список» неотличим от «все удалили» и от «первый заход на новом устройстве». Поэтому полный reload из таблицы (кнопка в настройках) карту чистит явно.

Генерация будущих эпизодов цикла (`ensureRecurringTasks`) тоже читает эту карту, иначе удалённый повтор на дату N создался бы заново при следующем открытии дня.

---

## Стек

| Технология | Зачем |
|---|---|
| React 18 | UI и состояние |
| TypeScript | типы задач, статусов, правил повтора, результата синка |
| Vite 6 | сборка |
| Tailwind CSS 3.4 | вёрстка и тёмная тема |
| vite-plugin-pwa / Workbox | service worker, precache, manifest |
| Lucide React | иконки |
| Google Apps Script | JSON API к таблице без своего сервера |
| GitHub Actions + Pages | сборка и публикация `dist` с `main` |

---

## CI/CD

Файл: [`.github/workflows/deploy.yml`](.github/workflows/deploy.yml).

Срабатывает на `push` в `main` и вручную (`workflow_dispatch`). Один job на `ubuntu-latest`:

1. `actions/checkout@v4`
2. Node 20 + кэш npm
3. `npm ci`
4. `npm run build` (`tsc && vite build`) — бандл, `sw.js`, `manifest.webmanifest`
5. `actions/configure-pages@v4`
6. `actions/upload-pages-artifact@v3` с `path: ./dist`
7. `actions/deploy-pages@v4`

Права: `pages: write`, `id-token: write`. `concurrency.group: pages`, `cancel-in-progress: true` — новый пуш отменяет незавершённый деплой.

В репозитории GitHub Pages должен быть включён с источником **GitHub Actions**, не ветка `gh-pages`. Публичный URL: `https://sofyapl.github.io/StepByStep/` (`base` в Vite завязан на имя репозитория).

---

## Установка

### Компьютер (Windows / Mac / Linux)

1. Открыть [приложение](https://sofyapl.github.io/StepByStep/) в Chrome, Edge или Яндекс Браузере.
2. Кнопка **«Установить на ПК»** в шапке или иконка установки в адресной строке.
3. Либо меню браузера:
   - Chrome: «Сохранить и поделиться» → «Установить приложение ШагЗаШагом»;
   - Edge: «Приложения» → «Установить этот сайт как приложение»;
   - Яндекс: «Дополнительно» → «Установить как приложение».

После установки — ярлык, меню «Пуск», закрепление в панели задач, окно без вкладок.

### Android (Chrome / Яндекс)

Меню `⋮` → «Установить приложение» / «Добавить на главный экран».

### iOS (Safari)

«Поделиться» → «На экран Домой».

---

## Синхронизация с Google Таблицей

1. Создать таблицу на [sheets.new](https://sheets.new).
2. **Расширения → Apps Script**.
3. В приложении: шестерёнка → скопировать шаблон скрипта → вставить в редактор → сохранить.
4. **Развернуть → Новое развертывание**: тип «Веб-приложение», доступ **Все** (`Anyone`). Иначе `fetch` из PWA на GitHub Pages не пройдёт.
5. URL `…/exec` вставить в настройки приложения → **Сохранить адрес**.
6. При желании включить автосинк при изменениях.

Первый запрос Google может показать предупреждение о непроверенном приложении — это скрипт в вашей же учётке, не OAuth-клиент стороннего разработчика.

---

## Локальный запуск

```bash
git clone https://github.com/SofyaPl/StepByStep.git
cd StepByStep
npm install
npm run dev
npm run build
```

---

## Лицензия

[MIT](LICENSE).
